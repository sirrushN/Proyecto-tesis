<div style="width:80%; margin:10px auto; position: relative;">

  <h1>Materias</h1>

  <table class="table-index">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @subjects.each do |subject| %>
        <tr>
          <td><%= subject.name %></td>
          <td><%= subject.description %></td>
          <td><%= link_to 'Mostrar', subject %></td>
          <td><%= link_to 'Editar', edit_subject_path(subject) %></td>
          <td><%= link_to 'Eliminar', subject, method: :delete, data: { confirm: 'Esta seguro?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Nuevo registro de materia', new_subject_path, class:"btn-new" %>
</div>
