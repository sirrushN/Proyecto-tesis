<%= form_with(model: subject) do |form| %>
  <% if subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject.errors.count, "error/es") %> de validacion sin resolver:</h2>

      <ul>
        <% subject.errors.each do |error| %>
          <li><%= error.message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre, class:"form-reg" %>
    <%= form.text_field :name, class:"form-reg" %>
  </div>

  <div class="field">
    <%= form.label :Descripción, class:"form-reg" %>
    <%= form.text_area :description, class:"form-area" %>
  </div>

  <div class="actions">
  <input type="submit" name="commit" value="Guardar" data-disable-with="Guardar" class="btn-save">
  </div>
<% end %>
